// Target thumbnail dimensions for each card layout's <ProxiedImage resize>,
// matching the fixed pixel sizes their CSS (dist/styles/cards.css) draws
// them at. Keep these in sync if that CSS changes.

export const DEFAULT_CARD_BG_SIZE = { width: 512, height: 528 }
export const DEFAULT_CARD_HEAD_SIZE = { width: 512, height: 288 }
export const MAGAZINE_CARD_THUMB_SIZE = { width: 400, height: 320 }
export const LIST_CARD_THUMB_SIZE = { width: 160, height: 160 }
