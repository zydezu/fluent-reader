// Target thumbnail dimensions for each card layout's <ProxiedImage resize>,
// matching the fixed pixel sizes their CSS (dist/styles/cards.css) draws
// them at. Keep these in sync if that CSS changes.

export const DEFAULT_CARD_BG_SIZE = { width: 256, height: 264 }
export const DEFAULT_CARD_HEAD_SIZE = { width: 256, height: 144 }
export const MAGAZINE_CARD_THUMB_SIZE = { width: 200, height: 160 }
export const LIST_CARD_THUMB_SIZE = { width: 80, height: 80 }
